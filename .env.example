# Base URL used by sitemap.ts and robots.ts (no trailing slash)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Enable the full demo subscriber seed outside local development (defaults to false in production)
CRATEMATCH_ENABLE_DEMO_SEED=false
